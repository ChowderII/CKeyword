#include <stdio.h>
#include <stdlib.h>
#include <pthread.h>

/* entry function to the threads */


int main()
{
	char *stringPointer = realloc();
	if (stringPointer != NULL){
		printf("success");
	}
	return 0;
}
